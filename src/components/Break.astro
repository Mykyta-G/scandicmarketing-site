<section class="break" aria-label="Bra marknadsföring märks. Vår mäts." data-field="wave">
  <div class="content">
    <p class="line display split">
      Bra marknadsföring <span class="hl">märks</span><span class="dot">.</span><br />
      Vår <span class="hl">mäts</span><span class="dot">.</span>
    </p>
    <a class="cta reveal" href="#kontakt">Boka ett samtal <span aria-hidden="true">→</span></a>
  </div>
</section>

<style>
  .break {
    position: relative;
    min-height: 72svh;
    display: grid;
    place-items: center;
    border-top: 1px solid var(--hairline);
    border-bottom: 1px solid var(--hairline);
  }

  .content {
    position: relative;
    text-align: center;
    padding: 3.5rem var(--gutter);
  }
  .line {
    color: var(--navy);
    font-size: clamp(1.9rem, 4.2vw, 3.4rem);
    text-shadow: 0 1px 20px rgba(255, 255, 255, 0.7); /* lyfter texten ur partikelvågen */
  }
  .line .dot { color: var(--accent); }

  .cta {
    display: inline-block;
    margin-top: 2.4rem;
    padding: 0.85rem 1.5rem;
    background: var(--accent);
    color: var(--cta-text);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: var(--radius);
    box-shadow: var(--shadow-soft);
    transition: background 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .cta:hover { background: var(--accent-strong); }
</style>
