---
const base = import.meta.env.BASE_URL.replace(/\/+$/, '');
---
<footer class="footer">
  <div class="rule"><div class="ticks" aria-hidden="true"></div></div>

  <div class="inner">
    <div class="col brand">
      <img src={`${base}/img/logo-white.webp`} alt="Scandic Marketing" width="129" height="40" />
      <p class="tagline">
        Digital marknadsföring som går att mäta —
        i Helsingborg och resten av Norden.
      </p>
      <ul class="contact-list">
        <li><a href="mailto:kontakt@scandicmarketing.se">kontakt@scandicmarketing.se</a></li>
        <li><a href="tel:+46769298501">+46 76 929 85 01</a></li>
        <li>Redaregatan 48, Helsingborg</li>
      </ul>
    </div>

    <nav class="col" aria-label="Snabblänkar">
      <p class="label head">Snabblänkar</p>
      <ul>
        <li><a href={`${base}/`}>Hem</a></li>
        <li><a href={`${base}/#tjanster`}>Tjänster</a></li>
        <li><a href={`${base}/#resultat`}>Resultat</a></li>
        <li><a href={`${base}/#kontakt`}>Kontakt</a></li>
      </ul>
    </nav>

    <div class="col">
      <p class="label head">Följ oss</p>
      <ul>
        <li><a href="https://www.instagram.com/scandicmarketing.se/" rel="noopener">Instagram</a></li>
      </ul>
      <p class="label head hours-head">Öppet</p>
      <p class="hours">Vardagar 09–17<br />Svar inom en arbetsdag</p>
      <a class="mini-cta" href={`${base}/booking`}>Boka ett samtal <span aria-hidden="true">→</span></a>
    </div>
  </div>

  <div class="bottom">
    <div class="bottom-inner">
      <p class="muted">© 2026 Scandic Marketing</p>
      <nav class="legal" aria-label="Juridik">
        <a href={`${base}/integritetspolicy`}>Integritetspolicy</a>
        <a href={`${base}/anvandarvillkor`}>Användarvillkor</a>
      </nav>
      <p class="label sig">56.04°N&nbsp;&nbsp;12.69°E</p>
    </div>
  </div>
</footer>

<style>
  /* Sidans enda mörka band — kundens egen hjältegradient bär avslutet.
     Lokala färger: allt härinne ligger på navy, inte på den vita marken. */
  .footer {
    --f-dim: #a9b6c9;   /* sekundär typ på navy — ~8:1 */
    --f-label: #9fb0c6; /* etiketter på navy — AA även på gradientens ljusa ände */
    --f-hair: rgba(238, 242, 247, 0.16);
    background: var(--gradient-hero);
    color: #eef2f7;
    margin-top: clamp(4rem, 9vh, 6rem);
  }
  .footer :global(.label) { color: var(--f-label); }

  .rule { border-top: 1px solid var(--f-hair); }
  .ticks {
    height: 7px;
    background: repeating-linear-gradient(to right, var(--f-hair) 0 1px, transparent 1px 72px);
    mask-image: linear-gradient(to right, transparent, black 8%, black 92%, transparent);
  }

  .inner {
    max-width: var(--page-max);
    margin-inline: auto;
    padding: 3.2rem var(--gutter) 2.6rem;
    display: grid;
    grid-template-columns: 1.6fr 1fr 1fr 1fr;
    gap: 3rem;
  }

  .brand img { height: 2.1rem; width: auto; }
  .tagline {
    margin-top: 1.2rem;
    color: var(--f-dim);
    font-size: 0.92rem;
    max-width: 22rem;
  }
  .contact-list {
    list-style: none;
    margin: 1.4rem 0 0;
    padding: 0;
  }
  .contact-list li {
    color: var(--f-dim);
    font-size: 0.92rem;
    padding: 0.25rem 0;
  }
  .contact-list a {
    color: var(--f-dim);
    text-decoration: none;
    transition: color 0.2s;
  }
  .contact-list a:hover { color: #93c5fd; }

  .head { margin-bottom: 1rem; }
  .col ul {
    list-style: none;
    margin: 0;
    padding: 0;
  }
  .col ul li { padding: 0.28rem 0; }
  .col ul a {
    color: var(--f-dim);
    font-size: 0.92rem;
    text-decoration: none;
    transition: color 0.2s;
  }
  .col ul a:hover { color: #93c5fd; }

  .hours-head { margin-top: 1.6rem; }
  .hours { color: var(--f-dim); font-size: 0.92rem; }
  .mini-cta {
    display: inline-block;
    margin-top: 1.5rem;
    padding: 0.7rem 1.2rem;
    background: var(--accent);
    color: var(--cta-text);
    font-family: var(--font);
    font-size: 0.7rem;
    font-weight: 500;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: var(--radius);
    transition: background 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .mini-cta:hover { background: var(--accent-strong); }

  .bottom { border-top: 1px solid var(--f-hair); }
  .bottom-inner {
    max-width: var(--page-max);
    margin-inline: auto;
    padding: 1.3rem var(--gutter);
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1.5rem;
    flex-wrap: wrap;
  }
  .muted { color: var(--f-dim); font-size: 0.82rem; }
  .legal { display: flex; gap: 1.6rem; }
  .legal a {
    color: var(--f-dim);
    font-size: 0.82rem;
    text-decoration: none;
    transition: color 0.2s;
  }
  .legal a:hover { color: #eef2f7; }
  .sig { color: var(--f-label); }

  @media (max-width: 900px) {
    .inner { grid-template-columns: 1fr 1fr; gap: 2.4rem; }
  }
  @media (max-width: 520px) {
    .inner { grid-template-columns: 1fr; }
  }
</style>
